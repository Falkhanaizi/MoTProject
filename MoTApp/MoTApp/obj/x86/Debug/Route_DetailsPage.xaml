<Page x:ConnectionId='1'
    x:Class="MoTApp.Route_DetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MoTApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:MoTApp"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Image x:ConnectionId='2' x:Name="BackBtn" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Width="25" Height="25" Source="Assets/icon_BackArrow2.png"                        />
        <TextBlock Text="Route Details" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="30" FontWeight="Bold"/>

        <Frame x:ConnectionId='3' x:Name="MyFrame" Grid.Row="1">
            
        </Frame>
        <GridView x:ConnectionId='4' x:Name="RoutesGridView" Grid.Row="1"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.HorizontalScrollMode="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Hidden"
                 ScrollViewer.VerticalScrollMode="Auto"
                  Padding="0"
                  HorizontalAlignment="Center"
                                                                   
                                                Background="Transparent">
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="8"/>
                </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemTemplate>
                <DataTemplate                        >
                    <RelativePanel x:ConnectionId='11'>
                        <Image x:ConnectionId='12' x:Name="mapImage"                            Width="100" Height="100" Stretch="UniformToFill"/>
                        <Border x:ConnectionId='13' Width="40" Height="40" 
                                                       
                            BorderBrush="White"
                            BorderThickness="2"
                                RelativePanel.AlignLeftWith="mapImage">
                            <TextBlock x:ConnectionId='14'                    Foreground="White"
                                   FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </RelativePanel>

                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <ListBox x:ConnectionId='5' x:Name="RoutesListBox"
                 Grid.Row="1"
                 VerticalAlignment="Bottom"
                 Visibility="Collapsed"
                 Background="Black"
                 Opacity="0.75"
                 Margin="0,0,0,10"
                 Padding="5"
                 ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                 ScrollViewer.HorizontalScrollMode="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollMode="Disabled"
                                                                  
                                               >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="5,0,5,0"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate                        >
                    <Border x:ConnectionId='9' Width="40" Height="40" 
                                                       
                            BorderBrush="White"
                            BorderThickness="2">
                        <TextBlock x:ConnectionId='10'                    Foreground="White"
                                   FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <RelativePanel Grid.Row="2" Background="#FFA42226">
            <Border x:ConnectionId='6' Margin="5" x:Name="RouteIdBtn" Width="40" Height="40" IsTapEnabled="True"                           
                            Background="Gray" BorderBrush="White"
                            BorderThickness="2" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True">
                <TextBlock x:ConnectionId='8'  x:Name="idText" Text="All" Foreground="White" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <Button x:ConnectionId='7' x:Name="RouteMapBtn" Content="Route Map"                           Background="White" Height="50" FontSize="18" Width="130" RelativePanel.AlignRightWithPanel="True"/>
        </RelativePanel>
        
    </Grid>
</Page>

